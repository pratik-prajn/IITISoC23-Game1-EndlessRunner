using UnityEngine;

public class destroy : MonoBehaviour
{
    
    public GameObject player;
    
    // Update is called once per frame
    void Update()
    {
    

      /* if (player.transform.position.z>Math.Abs(gameObject.transform.position.z))
        {
            Destroy(gameObject);
            
        }
*/
    }
}
